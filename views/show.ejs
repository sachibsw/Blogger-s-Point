<% include ./partials/header%>
<div style="margin-top:4.0em !important">
<div class="ui main text container" style="border-color:transparent">
      <div class="ui huge header"><%=blog.title%></div>
          <div class="ui top attatched">
            <div class="item">
               <img src="<%=blog.image%>" class="ui centered rounded image">
           </div>
             <div class="content"><span><%= blog.created.toDateString()%></span></div>
             <div class="description"><p><%=blog.body%></p> </div>
             <div class="content">
                <span class="description"><strong>Author:-</strong><%= blog.author%></a>
             </div>
        <div class="content">
          <span class="description"><strong>Category:-</strong><%= blog.category%></a>
        </div>
         </div>
        <a href="/blogs" class="ui inverted blue button">Go back</a>
       
        <% if(blog.author===cUser.username){%>
        <a href="/blogs/<%=blog._id%>/edit" class="ui inverted green button">EDIT</a>
       <form class="ui form delete" action="/blogs/<%=blog._id%>?_method=DELETE" method="POST">
           <button class="ui red inverted button">DELETE</button>
       </form>
       <% }%>
</div>
</div>
<% include ./partials/header%>